fetch('/api/scene-graph').then(r=>r.json()).then(data=>{['start','end'].forEach(id=>{const s=document.querySelector(`#${id}`);data.nodes.forEach(n=>s.add(new Option(n.label,n.id)));if(data.nodes.length)s.value=id==='start'?data.nodes[0].id:data.nodes[data.nodes.length-1].id});document.querySelector('#validate').onclick=async()=>{const r=await fetch('/api/validate-flow',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({start:document.querySelector('#start').value,end:document.querySelector('#end').value})});const x=await r.json();if(!x.ok){document.querySelector('#result').textContent=x.errors.map(e=>`${e.file}: ${e.message}`).join('\n');return}const route=(x.path||[]).join(' → ');const checked=(x.checked||[]).filter(file=>!(x.path||[]).includes(file));document.querySelector('#result').textContent=`検証OK\n経路: ${route}${checked.length?`\n同じ範囲内で検証した分岐: ${checked.join(', ')}`:''}`}}).catch(e=>document.querySelector('#result').textContent=e.message);
