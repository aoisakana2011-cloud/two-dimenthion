2026-09-08 言語仕様・実装・コンパイラ・build 横断監査

**修正状況（同日更新）: 下記14項目を修正済み。** 32件の監査回帰テストを追加し、既存分と合わせて91件をネイティブ実行有効・スキップなしで検証した。最終の初期化フロー調整後も監査回帰32件を再実行して成功した。`test/audit-regression.test.js` と `test/fixtures/audit-cases.json` に期待値付きの再現ケースを保存している。

辞書などの複合値はコピーで統一し、const、include初期化順、代入の評価順を `syntax-draft.md` に明記した。includeを展開したプログラムを各ファイルのpackageに格納し、初期化検査は実行命令とgoto時点の状態をたどる方式へ変更した。

TypeScript build、ネイティブRelease build、実ブラウザーでの主要な修正6ケースとconst診断、既存プレイヤー／エディターチェック、SDLスモークテストが成功した。現在のプロジェクトの修正後packageは `build/audit-20260908/fixed-main.nsp.json`。実機の描画・音声品質や別PCへの配布は今回も未検証。

以下は修正前の監査記録。行番号は監査時点のもの。

現行の未コミット変更を含む作業ツリーを対象に調査した。既存テストと現在のプロジェクトのpackは成功するが、追加の再現ケースから、以下の14項目の不具合・仕様不一致を確認した。P1は誤った結果の生成、環境間の結果不一致、基本的なbuildの阻害を優先したもの。P2は静的検査の漏れ、正当なコードの拒否、構文や変換の不一致である。

監査時点では実装の修正を行っていなかった。再現スクリプトは [build/audit-20260908/repro.cjs](build/audit-20260908/repro.cjs)、修正前の入力・診断・結果は [results-before.json](build/audit-20260908/results-before.json) に保存した。再実行は `node build/audit-20260908/repro.cjs`。このスクリプトは結果収集用であり、成功終了は不具合の解消を意味しない。JSのBigIntは結果JSON内で文字列として記録する。修正後の合否は `test/audit-regression.test.js` の期待値付きテストで検証する。

1. **[P1] ループ内の条件が初回の値で固定され、実行結果が変わる。**

   対象: [src/compiler/compiler.ts:210](src/compiler/compiler.ts#L210)。ケース `loop`。

   ```tds
   int x = 0
   int hits = 0
   while x < 3 {
     if x == 0 { set hits = hits + 1 }
     set x = x + 1
   }
   ```

   期待は `hits = 1`。実際はJS・ネイティブとも `hits = 3`。ループ本体を最適化する前に反復で変化する値を無効化せず、外側の `x = 0` を本体へ渡すため、条件が除去される。`for` にも同じ処理がある。ループ入口・反復間・ループ変数のスコープを考慮する必要がある。

2. **[P1] 関数の副作用を反映する前に条件分岐を最適化する。**

   対象: [src/compiler/compiler.ts:173](src/compiler/compiler.ts#L173)、[同:197](src/compiler/compiler.ts#L197)。ケース `condition_effect`、`argument_effect`。

   `x = 0` の後、`change()` が `x = 1` に変更する。`if change() == 1` の内部で `x == 0` を判定すると、期待値1に対して結果99になる。`consume(change())` の後の判定でも同様。両実行環境で再現した。条件評価の副作用を分岐本体へ渡す前に反映しておらず、単独call文では引数内の関数呼び出しを無効化処理が走査していない。

3. **[P1] 混在型の辞書を `dict[str]` と判定し、型の保証が壊れる。**

   対象: [src/checker/type-checker.ts:170](src/checker/type-checker.ts#L170)。ケース `dictionary_mixed`。

   `dict[str] a = {"x": 1, "y": "s"}` と `str result = a["x"]` がエラーなしでコンパイルされる。実行後の `result` は文字列ではなく整数。仕様8.1の値型統一に反する。型が混在した場合にエラーではなく辞書の値型 `str` を返している。

4. **[P1] include先のグローバル宣言が重複扱いになり、packできない。**

   対象: [tools/pack.js:111](tools/pack.js#L111)、[tools/project.js:69](tools/project.js#L69)。ケース `include_global`、`include_order`。

   `main.tds` が `include common.tds` を持ち、`common.tds` に `int x = 1` を置いただけで `x` の再宣言エラーになる。プロジェクト全体から集めた外部変数表から現在ファイルの宣言しか除外せず、その後includeを展開するため、同じ宣言が外部変数とローカル宣言の両方に現れる。include先の宣言を識別できる所有元情報が必要。

5. **[P1] gotoより後の未実行宣言を初期化済みと扱い、壊れたpackageを生成する。**

   対象: [tools/pack.js:63](tools/pack.js#L63)、[同:91](tools/pack.js#L91)。ケース `init_after_goto`。

   `main.tds` を `goto next.tds`、その次の行を `int x = 1` とし、`next.tds` で `x` を参照するとpackは成功する。しかし両ランタイムが未定義変数 `x` で停止する。ファイル内に宣言があれば、その宣言が実際に実行されたかを問わず、すべての外部gotoへ初期化済みとして伝播している。ファイル単位ではなく、gotoに到達する経路ごとの定義状態が必要。

6. **[P1] 辞書の代入・関数引数がJSでは参照共有、ネイティブではコピーになる。**

   対象: [Edit/runtime.js:56](Edit/runtime.js#L56)、[同:104](Edit/runtime.js#L104)、[native/runtime.hpp:58](native/runtime.hpp#L58)、[同:150](native/runtime.hpp#L150)。ケース `dictionary_alias`、`dictionary_parameter`、`const_alias`。

   `dict[int] b = a` の後に `b["x"] = 2` とすると、`a["x"]` はJSで2、ネイティブで1。辞書を引数に渡して変更した場合も同じ差が出る。またJSではconst辞書を別名へ代入してから変更でき、const元まで書き換わる。仕様に複合型のコピー・共有規則を定め、両実装とconstの扱いを揃える必要がある。

7. **[P1] ネイティブの辞書代入が右辺の副作用を消してしまう。**

   対象: [native/runtime.hpp:183](native/runtime.hpp#L183)。ケース `dictionary_side_effect`。

   `set d["x"] = change()` の `change()` が `d["y"] = 9` を実行する場合、JSでは `{x:1,y:9}`、ネイティブでは `{x:1,y:0}` になる。ネイティブは右辺評価前に辞書をコピーし、右辺評価後に古いコピーを書き戻す。単純な値渡しの違いとは別に、同じグローバル辞書への更新が失われる問題。

8. **[P2] constの静的検査が操作・分岐・ファイル・スコープで一致しない。**

   対象: [src/checker/type-checker.ts:468](src/checker/type-checker.ts#L468)、[同:490](src/checker/type-checker.ts#L490)、[同:818](src/checker/type-checker.ts#L818)、[tools/pack.js:33](tools/pack.js#L33)。ケース `const_unset`、`const_branch`、`external_const`、`const_shadow`。

   const辞書への `unset`、全分岐でconst宣言した変数への後続 `set`、別ファイルのconstへの `set` が静的検査を通り、実行時に両環境で停止する。一方、グローバルconstと同名の関数引数を更新すると、本来は別のローカル変数なのにconst変更として拒否する。readonly情報を変数名だけでなく束縛・スコープ・外部契約に結び付ける必要がある。

9. **[P2] sayブロック内の再帰呼び出しが検出されない。**

   対象: [src/checker/type-checker.ts:592](src/checker/type-checker.ts#L592)。ケース `recursion_say`。

   `fn f() -> str { say { f() } ... }` をエラーなしでコンパイルできる。仕様17で再帰は禁止されているが、呼び出しグラフの走査に `sayBlock` がない。`unset` のキー式も走査対象から漏れている。sayの再帰についてコンパイル成功を確認した。無限再帰の実行は行っていない。

10. **[P2] 有限whileを終了しない経路と判断し、戻り値不足を見逃す。**

    対象: [src/checker/analyzer.ts:211](src/checker/analyzer.ts#L211)、[同:451](src/checker/analyzer.ts#L451)。ケース `finite_while_return`。

    `int x = 0; while x < 1 { set x = x + 1 }` に相当する改行区切りの関数本体は有限なのに、初回条件が真という理由で後続へ進まないと判定される。`-> int` 関数にreturnがなくてもコンパイルされ、実行時に「値を返さなかった」と両環境で停止する。同じ判定は到達不能表示にも使われる。

11. **[P2] returnする分岐まで変数定義の合流対象に含め、正当な関数を拒否する。**

    対象: [src/checker/type-checker.ts:490](src/checker/type-checker.ts#L490)。ケース `reachable_return`。

    `if n == 1 { return 1 } else { int x = 2 }` の後の `return x` が未定義エラーになる。後続へ到達する唯一の分岐ではxが定義されており、仕様9.3の「すべての到達可能な分岐」という規則に反する。戻らない分岐を合流から除く必要がある。

12. **[P2] `int("+-1")` の成功・失敗がランタイムで異なる。**

    対象: [native/runtime.hpp:17](native/runtime.hpp#L17)、[Edit/runtime.js:14](Edit/runtime.js#L14)。ケース `conversion`。

    JSは変換エラー、ネイティブは-1として受理する。ネイティブは先頭の `+` を除いてから符号付き変換へ渡すため、2つの符号が通る。変換の入力文法を揃える必要がある。

13. **[P2] 型を明記した空の文字列辞書が宣言できない。**

    対象: [src/checker/type-checker.ts:169](src/checker/type-checker.ts#L169)。ケース `dictionary_empty`。

    `dict[str] a = {}` が `dict[int]` との不一致で拒否される。空辞書の型を常にintに決めており、宣言側の型を使わない。空の辞書を作り、後から文字列キー／値を追加する通常の使い方ができない。

14. **[P2] 文書が許可する構文とパーサーの受付範囲が異なる。**

    対象: [src/parser/parser.ts:98](src/parser/parser.ts#L98)、[同:125](src/parser/parser.ts#L125)、[同:276](src/parser/parser.ts#L276)、[同:291](src/parser/parser.ts#L291)。ケース `newline_character`、`newline_struct`、`goto_hyphen`、`say_expression`。

    仕様1は開き波括弧を次行に置けるとしているが、character・structで構文エラーになる。仕様4がパス文字として認めるハイフンを含む `goto chapter-1.tds` は引用符なしで解析できない。仕様12が認める話者省略の式 `say message + "!"` はmessageを話者と誤認する。それぞれ、現状では波括弧を同じ行に置く、goto先を引用符で囲む、`say narrator ...` と明示する回避策がある。

確認した正常系と範囲:

- `npm.cmd test`: 59件中58件成功、ネイティブ1件は環境変数未設定でスキップ。
- `cmake --build native/build-audit --config Release`: 成功。既存設定・既存依存関係を使った増分build。
- `NOVEL_NATIVE_EXE` を上記exeに設定して `node --test test/runtime.test.js`: 15件すべて成功、スキップなし。ネイティブ実行比較を含む。
- `node test/native-smoke.cjs`: 成功。SDLのdummy映像・音声ドライバーによる画像形式、音声形式、動画、フェード、choice、sayの実行。実画面・実スピーカーの品質確認ではない。
- `node test/browser-check.cjs`: Edgeのheadlessブラウザーで成功。画像／キャラクター表示、choice操作、関数呼び出し、エラー表示を確認。
- `node test/editor-analysis-check.cjs`: Edgeのheadlessブラウザーで成功。ライブ診断、補完、分割編集など既存チェックを確認。
- 現在の `Edit/scenes/main.tds` を `build/audit-20260908/current-main.nsp.json` へpack: 成功。
- 追加の再現ケースは実際のコンパイラ出力をJS Runtimeとネイティブheadless実行へ渡して比較した。追加ケースのJS実行はNode上であり、すべてをブラウザー画面上で操作したわけではない。
- 言語仕様、lexer/parser/AST、型検査、解析、最適化・メタデータ、project/include/pack、serverのcompile/build経路、player、native runtime/media、CMake/vcpkg/package設定、データschemaを確認した。

`struct_return` は関数のstruct戻り値を変数初期値にできないことを確認したが、仕様8.2が初期値を辞書形式に限定しているため、不具合件数には含めていない。`init_call` は初期化前参照を正しく拒否する対照ケース。include先structを取り込まない点も仕様4に明記されているため、未実装制約として扱った。

本監査は「すべての入力に不具合がない」ことの証明ではない。依存関係を新規取得するクリーンbuild、別OS・別PCへの配布、長時間のメディア再生、全コーデック・破損素材、実機の描画／音声品質は未検証。実装と仕様を揃える修正後には、上記の不具合ケースを期待値付きの回帰テストにする必要がある。

作業ツリーについて: 開始時点の未コミット変更を保持した。テストが生成した既存データJSON・既存スクリーンショットは退避内容に戻し、開始時点で変更のなかったスモークテストの追跡対象出力も戻した。本監査で追加したのはこの報告書と `build/audit-20260908/` の再現・結果・pack成果物である。
